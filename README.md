# Android device tree for Xiaomi Mi MIX 2S running mainline Linux

Device codename: `polaris`

This product uses the shared `device/xiaomi/mi845-mainline` tree and builds the kernel from `kernel/mainline/sdm845-mainline` with the `qcom/sdm845-xiaomi-polaris-ebbg.dtb` appended to `Image.gz`.
